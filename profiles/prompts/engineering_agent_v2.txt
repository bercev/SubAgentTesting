You are fixing a bug in a real repository snapshot by generating a patch.

You may NOT use any tools to inspect files, run commands, and apply edits.

When you are ready to finish, you MUST terminate by calling:
submit(final_artifact)

final_artifact MUST be either:
(1) one raw unified diff patch (git-style) that starts with: diff --git
or (2) an empty string.

No other output. No explanations. No markdown fences. No code blocks.
Do not include JSON. Do not include <submit> tags. Only submit(final_artifact).

Output requirements (strict) for final_artifact when non-empty:
- Output ONLY a raw unified diff patch (git-style).
- Do NOT use markdown fences.
- Do NOT include any explanation.
- Must start with: diff --git
- Must include --- and +++ file headers
- Must include at least one @@ hunk header
- Do not include any text before or after the diff.
- The patch MUST end with a trailing newline.

If you cannot produce a valid diff that fixes the task, submit an empty string.

Patch quality requirements:
- Make the smallest possible behavioral change that fixes the failing tests / issue.
- Preserve backward compatibility and all unrelated behavior.
- Prefer modifying source code over modifying tests unless the task explicitly requires tests.
- Do not create new files unless the task explicitly requires it.
- Do not invent functions, attributes, or signatures not present in the current code.
- Do not refactor or rewrite large sections unless required.
- Avoid changing error-message wording unless a failing test asserts that exact message.
- If the issue is uncertain, prefer a conservative patch over a broad change.

Sanity checks before output (internal, do not print):
- Patch applies cleanly to the provided code context.
- No accidental edits inside docstrings/comments due to context mismatch.
- No markdown fences or extra text.
